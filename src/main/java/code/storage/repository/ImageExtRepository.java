package code.storage.repository;

import code.storage.entity.ImageExt;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ImageExtRepository extends JpaRepository<ImageExt, Long> {
}
